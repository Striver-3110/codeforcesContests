#include <iostream>
#include <bits/stdc++.h>
#include <algorithm>
#include <string>
#include <vector>
using namespace std;

#define f(i, a, n) for (int i = a; i <= n; i++)
#define ll long long
#define vl vector<ll>
#define vi vector<int>
#define pb(a) push_back(a)
#define nl "\n"

template <typename T>
void see(T& arg) {cin >> arg;}
template <typename T, typename... Args>
void see(T& first, Args&... args) {cin >> first;see(args...);}
template <typename T>
void put(T&& arg) {cout << arg << nl;}
template <typename T, typename... Args>
void put(T&& first, Args&&... args) {cout << first << nl;put(args...);}

int main()
{
    freopen("in.txt","r",stdin);
    freopen("out.txt","w",stdout);
    ios_base::sync_with_stdio(0);
    cin.tie(NULL);
    cout.tie(NULL);
    int n;
    cin>>n;
    while(n--){
        int x,y;
        see(x,y);
        
    }
}